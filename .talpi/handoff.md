# Handoff

업데이트: 2026-08-22 — **새 런 진행 중.** 이전 런(비눗방울 오브제 포트폴리오
v1)은 2026-08-16 에 done 으로 끝났고, 그 산출물은
`.talpi/archive/2026-08-22/` 에 있다. journal.md 는 두 런을 이어서 기록한다.

## 이번 런은 무엇인가

공개 직전 라운드. 배포는 하지 않고 공개해도 괜찮은 상태로 만든다. 두 가지를
만든다.

- 키보드만으로 모든 작품에 도달하고 열 수 있을 것.
- 방울 씬을 거치지 않고도 작품 목록을 볼 수 있는 상시 경로가 있을 것.

둘 다 등록부(`src/works/registry.ts`)에서 파생되어야 하고, 작품을 추가할 때
손댈 곳이 늘어나면 안 된다.

## Done so far

- spec 승인(2026-08-22). plan 승인, 4 페이즈. conventions.md 와 codemap.md 도
  같이 확정.
- **페이즈 3 완료(5 스텝 + 검증자 지적 수정 1 커밋).** base 는
  `4f618c177327771c6acd1289430c64ce0d76a2a7` 다. B1·B2·B3 가 핀되고 초록이며,
  검증자가 낸 `[FIX]` 3 건(전부 확인 공백·구조 문제, 제품 동작 결함 없음)을
  해소했다. 테스트 141 개 통과, `bun run build` 통과.
  - 키 해석 순수 모듈 `src/keys/keyNav.ts` 와 배선 훅 셋
    (`useSceneKeyNav`·`useListKeyNav`·`useWorkExitKeyNav`), 공유 층
    `useLayerKeyDown`.
  - 씬이 탭 정거장 하나를 차지하고 방향키가 작품 방울을 순환한다. 목록은
    커서 상태를 들지 않고 DOM 초점이 곧 커서다. 작품 페이지의 Esc 는 등록부
    파생 라우팅에 딸려 붙는다.
  - 페이즈 3 보고를 사람에게 보냈다. 답을 기다리지 않고 페이즈 4 로 간다.
- **페이즈 2 완료(3 스텝 + 검증자 지적 수정 1 커밋).** base 는
  `c53434a14cf7a529997d9c5f5e15bc66d091594e` 다. B4 가 핀되고 초록이며,
  검증자가 낸 `[FIX]` 3 건 가운데 둘은 실제 동작 구멍이었고 전부 해소했다.
  테스트 85 개 통과, `bun run build` 통과.
  - 씬을 띄울 수 없으면 `/` 가 `/works` 로 갈아친다. `HomeFallback` 은
    삭제되었고 `HOME_TESTID` 는 씬 없는 환경에서 어디에도 뜨지 않는다.
  - 실행 중 씬이 무너지는 두 사건(컨텍스트 상실, 마운트 실패)을 문 하나로
    모았고 판정은 `decideSceneFallback` 한 곳에 있다.
  - 페이즈 2 보고를 사람에게 보냈다. 답을 기다리지 않고 페이즈 3 으로 간다.
- **페이즈 1 완료(4 스텝 + 검증자 지적 수정 1 커밋).** base 는
  `3fda4975d4504a9b75f4605e46e06675ebe576a5` 다. B5 가 핀되고 초록이며,
  검증자가 낸 `[FIX]` 3 건은 전부 해소했다(셋 다 제품 동작이 아니라 확인
  공백이었다). 테스트 52 개 통과, `bun run build` 통과.
  - 목록을 그리는 유일한 모듈 `src/works/WorksList.tsx` 가 슬라이드와 전체
    화면 두 모습을 씬 없이 렌더한다.
  - `/works` 가 `/` 의 자식 라우트로 들어갔고, 아이콘으로 열리고 바깥
    클릭·뒤로가기로 닫힌다. 열려 있는 동안 뒤 씬은 `inert` 로 잠긴다.
  - 페이즈 1 보고를 사람에게 보냈다. 답을 기다리지 않고 페이즈 2 로 넘어간다.

## Next step

**페이즈 4 스텝 1 부터.** base 는
`a40bbd200b77f4b1cf456dc6a026a2ae487fb464` 이고 journal 에 `phase 4 started`
가 찍혀 있다. plan.md 페이즈 4 의 체크박스는 전부 비어 있다. 스텝 1 은
B6(보조기술에 드러나는 표면)를 실패하는 테스트로 핀하는 일이다.

이것이 이 런의 마지막 페이즈다. 마지막 스텝이 `.talpi/manual-check.md` 를
쓰는 일인데, 그 파일은 이미 페이즈 1·2·3 이 각자 항목을 덧붙여 두었으므로
스텝 5 는 백지에서 쓰는 것이 아니라 **VoiceOver 절을 더하고 전체를 한 번
정리하는 일**이 된다.

B6 은 수단과 낭독 문구의 표현을 위임했다. 이름·설명·위치 정보가 붙어
있는지는 자동 테스트로 확인하고, 실제로 무엇이 들리는지는 사람이 VoiceOver
로 확인한다. 사람은 VoiceOver 를 처음 써 보므로 켜고 끄는 법을 함께 적어야
한다.

## 사람이 아직 답하지 않은 열린 질문

페이즈 1 보고에 이미 실어 보냈다. 답이 오면 그때 반영하고, 답이 없어도 런은
계속 간다. 페이즈 2·3·4 를 진행하면서 다시 물을 필요는 없다.

구현자가 올린 **contract candidate** 들이다. 계약을 새로 핀하는 일은 사람이
스펙 승인 자리에서 정하므로 여기서 손대지 않는다.

- 전체 화면 목록에 붙는 안내 문구(Requirement 36)를 B5 의 목록 표면 계약이
  함께 핀해야 하는가. 지금은 B4·B6 몫으로 두고 `WorksList` 의 props 타입에서
  뺐다.
- 슬라이드의 닫힘 동작(Esc·바깥 클릭, Requirement 16–21)을 B5 가 핀해야
  하는가. 지금은 핀 목록에 없어서 `onClose` 가 props 타입에 없고, 뒤 스텝이
  더하게 된다.
- 슬라이드가 `main` 랜드마크를 절대 그리지 않는다는 것을 계약으로 핀해야
  하는가. 슬라이드가 홈 위에 열려 있는 동안 씬 쪽 `main` 이 유일한 `main`
  으로 남는 문제다. 지금 variant 분기는 제목·태그라인 유무만 핀되어 있다.
- 목록을 열고 닫아도 Canvas 가 재마운트되지 않는다는 성질(Requirement 32)을
  핀해야 하는가. `/works` 를 `/` 의 자식 라우트로 둔 덕에 성립하는데, 나중에
  라우트 구조를 평평하게 되돌리면 조용히 깨진다. 확인하려면 R3F `Canvas` 를
  스텁하고 `isWebGLAvailable` 을 모킹해야 해서 씬 없는 seam 만으로는 닿지
  않는다. 페이즈 3 의 B3 핀에서 검토할 값이 있다.
- 전체 화면 목록이 `onDismiss` 를 무시한다는 것(Requirement 22)을 핀해야
  하는가. 지금은 구조로만 지켜지고 있어서, 나중에 dismiss 면을 두 모습에
  모두 그리도록 고치면 씬 없는 홈이 닫히는 화면이 된다.

## 페이즈 2 보고에 실을 것

- **contract candidate**: `decideSceneFallback` 을 화면 없이 핀하는
  `src/scene/sceneFallback.test.ts` 를 둘 것인가. 두 스텝의 구현자가 모두
  올렸다. 갈래가 여섯으로 늘었고, 무엇보다 B4 가 못박은 "복구되어도
  되돌아가지 않는다"가 이제 이 함수의 한 줄로 표현되는데 그 줄은 컴포넌트를
  통해서도 확인할 수 없다(jsdom 에서 씬이 뜨지 않는다).
- **contract candidate(약함)**: `focusWorksList` 의 "빈 등록부에서도 초점이
  `<body>` 로 떨어지지 않는다". 페이즈 3 이 이 함수를 확장하며 자기 계약을
  얹을 가능성이 있어 지금 핀하면 곧 다시 쓰게 될 수도 있다.
- 씬이 무너진 뒤 작품 페이지에 들어갔다 홈 링크로 돌아오면 씬이 다시 뜰 수
  있다. `/works/<slug>` 는 최상위 형제 라우트라 `Home` 이 언마운트되고
  돌아올 때 프로브가 새로 돈다. B4 가 금지한 것은 화면이 **스스로** 다시
  뒤집히는 것이고 이건 방문자가 직접 이동한 결과라 위반이 아니라고 보았다.
  다만 Ledger 의 "어느 쪽이 홈인지가 세션 도중에 바뀔 수 있다"에 닿는다.
- `/works` 에서 씬이 무너지면 그 화면은 닫을 수 없는 전체 화면 홈이 된다.
  Ledger 가 알고 치른다고 적어 둔 그대로다. 페이즈 3 이 Esc 를 얹을 때 이
  상태에서 Esc 가 조용히 무력해지는 것이 의도한 그대로인지 다시 볼 자리다.

- Requirement 37 을 글자 그대로 핀했더니 이런 화면이 생긴다. **WebGL 을 쓸 수
  없는 방문자가 작품 페이지에서 "← 방울들에게로" 를 누르면 홈이 아니라 작품
  목록에 아무 설명 없이 도착한다.** 스펙이 "스스로 누른 링크이므로 안내가
  없다"고 명시했고 Reversibility Ledger 의 Decided 항목과도 어긋나지 않아
  분쟁으로 올리지 않았다. 다만 그 방문자에게는 "홈을 눌렀는데 홈이 아니다"가
  되므로 사람이 한 번 확인해 둘 값이 있다.

## 사람에게 물을 것 (페이즈 1 보고)

- 좁은 화면에서 슬라이드가 `width: min(26rem, 100%)` 이라 화면을 가득
  덮으므로 "바깥"이 남지 않는다. 그 방문자는 브라우저 뒤로가기(페이즈 3
  이후에는 Esc)로만 목록을 닫을 수 있다. 그대로 두어도 괜찮은지, 아니면 좁은
  화면에서 슬라이드가 폭을 조금 양보해 씬이 한 줄이라도 남아야 하는지 정해야
  한다. B5 의 "Esc 와 바깥 클릭으로 닫힌다"에 걸리는 문제라 구현자가 정하지
  않고 남겼다.

## 페이즈 3 보고에 실을 것

- **아이콘으로 슬라이드를 열면 아이콘이 언마운트되면서 초점이 `<body>` 로
  떨어진다.** 탭 고리가 이것을 받아 첫 탭이 초점을 목록으로 끌어오고,
  Requirement 5 가 "열리자마자 초점을 끌어가지 않는다"고 정해 두어서 스텝 4
  구현자가 손대지 않았다. 다만 키보드 방문자는 목록을 연 뒤 첫 탭에서
  `<body>` 를 한 번 거치게 되고, Failure Behavior 의 "초점이 사라진 요소에
  남지 않는다"에 스친다. 목록을 열 때 초점을 목록 표면으로 옮길지는 사람이
  정할 값이 있다.
- **contract candidate**: 씬이 뜨는 방문자의 Esc 갈래(방울 초점 복원, 씬이
  그새 불가능해졌을 때의 목적지 재파생)에 영구 테스트가 없다. 스텝 5
  구현자가 `isWebGLAvailable` 과 R3F `Canvas` 를 갈아 끼우면 jsdom 에서도
  핀할 수 있다는 것을 확인했다(그 형태로 확인한 뒤 지웠다). 그 형태를 계약으로
  받아들일지는 사람이 정한다.
- **contract candidate 넷** (스텝 4 구현자가 올림. 전부 B5 seam 으로 jsdom 에서
  확인 가능한데 지금 영구 핀이 없다): 슬라이드의 Esc 가 나가는 길을 정확히 한
  번 부르고 전체 화면의 Esc 는 아무 일도 하지 않는 것(20·22), 방향키 순환이
  **두 모습 모두에서** 실제로 배선되어 있는 것(17·24 — `keyNav.test.ts` 는
  산술만 핀한다), 슬라이드 탭 고리의 가장자리 되돌기(17), 빈 등록부에서
  방향키가 초점을 옮기지 않는 것(29).

- 전체 화면 목록에서 **엔터를 누가 처리하는가**. 목록 항목은 진짜 링크라
  브라우저가 활성화하지만, 씬의 방울에는 그런 기본 동작이 없어 전역 처리가
  필요하다. 두 경로가 같은 코드를 쓰면 링크에서 이중 발동 위험이 생기고,
  나누면 "키의 뜻은 하나"라는 계약의 글자와 어긋나 보인다. 스텝 1 의 핀은
  "전역 처리가 스스로 이동했다면 기본 동작을 반드시 막았어야 한다"로 두
  갈래를 모두 허용해 두었다.

## 페이즈 4 보고에 실을 것

- **B6 ⑥ 의 두 문구를 다른 강도로 핀했다.** 씬 폴백 안내는 "알려진다"(live
  region 또는 초점 이동)로, 빈 등록부 문구는 "낭독 표면에 존재한다"로
  잡았다. 근거는 방문자가 겪는 일이 다르다는 것이다 — 폴백 안내는 요청하지
  않은 화면 변화의 설명이라 스스로 찾아 읽을 이유가 없고, 빈 등록부 문구는
  스스로 연 목록 안에 있어 목록에 닿으면 읽힌다. 스펙 29 의 "그 사실이
  **소리로도** 전달된다"를 폴백 안내와 같은 강도로 읽어야 한다면 빈 문구
  쪽도 알림 요구로 올려야 하고, 그러면 지금 통과하는 핀 두 개가 실패로
  바뀐다.

## Gotchas

- **목록이 열리면 아이콘이 렌더되지 않으므로 `aria-expanded` 로 열림 상태를
  알리는 표준 수단을 쓸 수 없다.** 남는 것은 초점을 목록 안으로 옮기거나
  실시간 알림을 두는 것이다. 초점을 옮기는 선택은 Requirement 20(Esc 로
  닫으면 초점이 아이콘으로 돌아감, `sceneShell.test.tsx` 가 이미 핀함)과
  맞물리므로 함께 봐야 한다.

- **`src/keys/useSceneKeyNav.ts` 의 `focusedByKeyboard(element)` 는 브라우저의
  `:focus-visible` 판정이다.** 스텝 4 가 목록에서도 재사용할 만하다.
- **`keyNav` 는 이벤트 타깃을 모른다.** 목록 안에 버튼 같은 요소가 생기면
  스페이스 `preventDefault` 가 그 요소의 표준 동작을 가로챌 수 있으므로,
  타깃 판별은 리스너를 다는 쪽이 해야 한다. 지금 `WorksList` 카드는 `<a>` 라
  당장 걸리는 자리는 없다.

- **씬 없는 환경에서 `HOME_TESTID` 는 이제 어디에도 뜨지 않는다.** 페이즈 2
  스텝 2 가 `/` 를 `/works` 로 갈아치우면서 `HomeFallback` 이 사라졌기
  때문이다. 라우팅 단언은 `topRoutePath`
  (`router.state.matches[0].route.path`)로 옮겨 두었다. 앞으로 "홈으로
  간다"를 확인할 때 `HOME_TESTID` 를 쓰면 항상 null 이라 공허하게 통과한다.
- **`/works` 라우트 요소는 `<WorksList variant="fullscreen" />` 하나로 둔다.**
  `fullscreen` 이 이미 `<main>` 을 그리므로 바깥에서 또 감싸면 `main` 이
  둘이 된다.

- **jsdom 은 언제나 WebGL 이 없다.** `isWebGLAvailable()` 이 항상 false 라
  테스트는 늘 "씬을 못 띄우는" 경로를 탄다. 페이즈 2 이후에는 그 경로가
  `/works` 전체 화면 목록이 되므로, 기존 라우팅 테스트의 "홈으로 간다" 단언이
  `/` 와 `/works` 를 구별할 수 있어야 한다. 목록 화면에 자기 표식이 필요한
  이유다.
- **`getContext` 를 참으로 속이지 말 것.** R3F 캔버스가 진짜로 마운트되면서
  three.js 가 죽는다. 이전 런이 `src/scene/webgl.ts` 프로브를 Canvas 마운트
  *전* 게이트로 둔 것도 같은 이유다.
- **`src/test-setup.ts` 를 지우지 말 것.** jsdom 의 AbortController 를 Node
  네이티브로 교체하는데, 없으면 react-router 리다이렉트 테스트가 깨진다.
- **`bun run build` 가 가장 싼 기계적 확인이다** (`tsc -b && vite build`).
- **`tsconfig.node.json` 의 include 에 `src/theme.ts` 가 들어 있다.** vite
  설정이 `transformIndexHtml` 로 사이트 제목을 치환하느라 theme 을 임포트해서
  생긴 프로젝트 경계다. 건드리면 `tsc -b` 가 TS6307 로 실패한다.
- **이전 런의 규약 문서 원본**은 `.talpi/archive/2026-08-22/conventions-prev-run.md`
  에 있다. 거기 있던 모듈 지도는 `.talpi/codemap.md` 로 옮겼다.
